backend:
  name: git-gateway
  repo: freie-schule-bergmeilen/website
  branch: master # Branch to update (optional; defaults to master)

media_folder: "/static/img"
public_folder: "img"

collections:

  - name: "news" # Used in routes, e.g., /admin/collections/blog
    label: "Aktuelles" # Used in the UI
    folder: "src/pages/news" # The path to the folder where the documents are stored
    create: true # Allow users to create new documents in this collection
    slug: "{{year}}-{{month}}-{{day}}-{{slug}}" # Filename template, e.g., YYYY-MM-DD-title.md
    fields: # The fields for each document, usually in front matter
      - {name: kind, widget: hidden, default: "news"}
      - {label: "Layout", name: "layout", widget: "hidden", default: "news"}
      - {label: "Pfad", name: "path", widget: "string"}
      - {label: "Titel", name: "title", widget: "string"}
      #- {label: "Image", name: "image", widget: "image", required: false}
      - {label: "Datum", name: "date", widget: "datetime"}
      - {label: "Pin", name: "pin", widget: "boolean"}
      - {label: "Text", name: "body", widget: "markdown"}
      - {label: "Template", name: "templateKey", widget: "hidden", default: "news-item" }

  - name: "fragments"
    label: "Fragmente"
    folder: "src/fragments"
    create: false
    slug: "{{slug}}"
    fields:
      - {name: dataKind, widget: hidden, default: "fragment"}
      - {label: "Titel", name: "title", widget: "string", required: false}
      - {label: "Text", name: "body", widget: "markdown"}
      - {label: "Path", name: "path", widget: "hidden"}

  - name: team
    label: Team
    folder: "src/pages/team"
    create: true
    slug: "{{slug}}"
    fields:
      - {name: dataKind, widget: hidden, default: "team-member"}
      - {name: name, label: Name}
      - {name: position, label: "Funktion and der Schule"}
      - {name: experience, label: "Ausbildung/Berufserfahrung", required: false}
      - {name: photo, label: Foto, widget: image, required: false}
      - name: children
        label: Kinder
        widget: list
        required: false
        fields:
          - {label: Name, name: name, widget: string}
          - {label: Year, name: year, widget: string}
      - {name: order, label: Platz, widget: number, required: false}
      - {name: body, label: Bio, widget: "text", required: false}


  - name: "settings"
    label: "Sonstiges"
    delete: false
    editor:
      preview: false
    files:
      - name: "general"
        label: "Einstellungen"
        file: "data/settings.json"
        description: "Globale Einstellungen"
        fields:
          - {label: "Global title", name: siteTitle, widget: "string"}
          - label: "Aktuelles"
            name: news
            widget: object
            fields:
              - {label: "Anzahl News auf Homeseite", name: newsLimit, widget: number}

      - name: "goals"
        label: Ziele
        file: "data/goals.yml"
        description: "Ziele"
        fields:
          - {label: "Überschrift", name: title, widget: "string"}
          - name: goals
            label: Ziele
            widget: list
            fields:
              - {label: "Überschrift", name: title, widget: string}
              - {label: Beschreibung, name: description, widget: text, required: false}

      - name: "team-members"
        label: "Team Mitglieder"
        file: "data/team-members.yml"
        description: "Team members"
        fields:
          - name: teamMembers
            label: "Team Mitglieder"
            widget: list
            fields:
              - {name: name, label: Name}
              - {name: position, label: "Funktion and der Schule"}
              - {name: experience, label: "Ausbildung/Berufserfahrung", required: false}
              - {name: photo, label: Foto, widget: image, required: false, class: "team-member" }
              - name: children
                label: Kinder
                widget: list
                required: false
                fields:
                  - {label: Name, name: name, widget: string}
                  - {label: Year, name: year, widget: string}
              - {name: bio, label: Bio, widget: "text", required: false}


#  - name: "about" # Used in routes, e.g., /admin/collections/blog
#    label: "Über uns" # Used in the UI
#    folder: "src/pages/about" # The path to the folder where the documents are stored
#    create: true # Allow users to create new documents in this collection
#    slug: "{{slug}}" # Filename template, e.g., YYYY-MM-DD-title.md
#    fields: # The fields for each document, usually in front matter
#      - {label: "Layout", name: "layout", widget: "hidden", default: "page"}
#      - {label: "Path", name: "path", widget: "hidden", default: "/about"}
#      - {label: "Title", name: "title", widget: "string"}
#      - {label: "Body", name: "body", widget: "markdown"}
#      - {label: "Template", name: "templateKey", widget: "hidden", default: "page" }
